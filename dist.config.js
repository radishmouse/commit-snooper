// acquire a token here: https://github.com/settings/tokens
const token = 'token-goes-here';

// list repos here like so:
const repos = [
  'radishmouse/commit-snooper'
];

module.exports = {
  repos,
  token
};

